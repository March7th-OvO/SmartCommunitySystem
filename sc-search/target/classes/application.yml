server:
  port: 8600

spring:
  application:
    name: sc-search
  elasticsearch:
    uris: http://localhost:9200
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

security:
  jwt:
    secret: "smart-community-jwt-secret-please-change"
    expire-seconds: 7200

